# Инцидент 2026-06-09: тихое исчезновение лиза Suite 344 (Pinellas Park)

**Статус: RESOLVED 2026-06-10 ~03:38Z** — здание восстановлено из PITR, дыра закрыта деплоем, стабильность подтверждена. Артефакты: `~/sfa-incident-2026-06-09/`.

## Хронология (UTC)

| Когда | Что |
|---|---|
| 06-08 17:05–17:14 | Полный лиз-флоу Suite 344: DocuSign sent → инвойсы (депозит $550 + рент $293.33) → lease completed → PDF в Storage. Арендатор **Parsean Kaffashin** (sean@vbuildcustomhomes.com), $550/мес, 2026-06-15 → 2031-06-30 |
| 06-08 22:01:54 | stripeWebhook отметил депозит оплаченным → серверное зеркало `_mirrorBuildingV2CF` (прод-билд 13207f2) сделало full-set **без `_savedRev`** → монотонный гард b1 обнулён (дыра FIXES_LOG 67, фикс был в main, но не задеплоен) |
| **06-09 12:30–12:45** | **Clobber**: устаревшая клиентская вкладка (контент ≤4 июня: без кода PIN, без лиза) записала зеркало с `_savedRev=2` — rules пропустили (2 ≥ 0). Всё здание b1 откатилось к ≤4 июня. Той же волной снесены `doc.code` у Pasadena (SOU) и Bay Vista (BAY). Аудит-события нет — зеркала не аудируются |
| 06-09 — день | New Tampa (rev 30013) и Tech Data (rev 228) **отбили** stale-записи — гард работает там, где CF не стёр пол. Это контрольное доказательство механизма |
| 06-10 03:08 | Ночная вкладка Tony пере-зеркалила уже затёртое состояние (вторичный след) |
| 06-10 03:34–03:36 | **Деплой functions** (Entry 67 фикс + PP-вебхук DORMANT + excelId-фильтр экспорта) |
| 06-10 03:38 | **Restore**: b1 ← PITR-копия `2026-06-09T12:30:00Z` + `_savedRev=50000` + `_mirroredBy='incident-restore-2026-06-10'`; Pasadena/Bay Vista — возврат `doc.code` + пол 50000. 11/11 проверок ✓, стабильность ✓ |

## Причина

Связка двух известных дефектов: **Entry 67** (серверное зеркало стирает `_savedRev`) обнулила **Entry 65 Layer 2** (монотонный rules-гард) — и stale-вкладка прошла. Исключены доказательно: ghost-cleanup (разовый скрипт 23 мая, писателя в коде нет), `charge_failed`-обработчик (пишет только статус платежа), все авто-репейры (заморожены, проверено в живом билде 2d155da).

## Урон / восстановление

- **Восстановлено полностью**: лиз 344 (tenant/contractRent/депозит-штамп/leaseStart), код PIN, всё содержимое b1 на момент 12:30Z 06-09; коды SOU/BAY.
- **Не терялось вообще**: Stripe-инвойсы и оплата, contract-рекорд в монолите, DocuSign PDF, аудит-трейл, payments-коллекция.
- **Хвосты** (не критично): (1) stripe-штампы юнитов 429/341/354 — их mark-paid 06-09 19:34/20:54/22:08Z легли на затёртый док и в restore-копию не попали; rent-записи живы в payments-коллекции (история/биллинг работают через read-merge), штампы самовосстановятся при следующем инвойс-цикле или `sfaReconcile`; (2) правки architecture@ по b1 после 12:30Z 06-09 (если были) — повторить руками.

## Уроки / действия

1. ✅ **Деплой Entry 67** — серверные зеркала больше не стирают `_savedRev` (транзакция, preserve+advance).
2. ✅ Полы всех повреждённых зданий подняты до 50000.
3. ⚠️ **Бэкапы-снапшоты бесполезны для зданий под strip-ON** (хранят `buildings:[]`) — PITR был единственной сеткой. → Отдельная задача: backup CF должен снапшотить коллекции.
4. ⚠️ **Firestore Data Access audit выключен** — автора затирающей записи в GCP не видно. → Включить (write-аудит).
5. 🔜 Постоянная защита от stale-вкладок (CAS по applied-контенту, карантин импорта, ресинк на пробуждении, алерт на падение occupancy) — дизайн на адверсариальной проверке, отдельное решение.

## Rollback восстановления (если понадобится)

Пре-restore копии: `~/sfa-incident-2026-06-09/pre-restore-{b1,b1776578294270,b1778422024964}.json` — вернуть REST-PATCH'ем (но это вернёт затёртое состояние; PITR-окно на 06-09 живо до ~16 июня).
