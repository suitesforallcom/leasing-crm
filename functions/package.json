{
  "name": "suitesforall-functions",
  "description": "Cloud Functions for SuitesForAll: Stripe invoicing + webhook handlers",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"(lint skipped — pure vanilla JS, no toolchain yet)\"",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "googleapis": "^144.0.0",
    "stripe": "^17.5.0"
  },
  "devDependencies": {}
}
